# auroraops-base Context

## 1. 目标与定位
`auroraops-base` 为 AuroraOps 架构中最基础、最通用的底层系统基座子仓库。负责将一台裸金属/云主机初始化为标准受管状态。

## 2. 核心架构约束
1. **纯净性**：本仓库只提供基础设施底盘，不包含任何具体上层服务或代理。
2. **依赖锁定**：所有外部依赖在 `requirements.yml` 中锁定精确版本（`ansible.posix: 2.1.0`，`community.general: 13.1.0`）。
3. **环境无关性**：脚本或配置不得硬编码特定机器路径（如旧 monolith 路径 `/root/AuroraOps` 已全面剥离，统一使用可配置变量）。
4. **单向引用**：本仓库作为能力提供方，绝不直接依赖父仓或其他同级子仓。
