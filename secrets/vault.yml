---
# Public placeholder vault file.
# Replace with real secrets in private environments only.
vault_ansible_become_password: "CHANGEME_VAULT_ANSIBLE_BECOME_PASSWORD"
vault_pass: "CHANGEME_VAULT_PASS"
vault_password_file: "CHANGEME_VAULT_PASSWORD_FILE"
vault_root_password: "CHANGEME_VAULT_ROOT_PASSWORD"
